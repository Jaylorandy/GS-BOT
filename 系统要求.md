# 🔧 系统要求和依赖说明

## ✅ 系统要求

### 必须安装的组件

#### 1. **Google Chrome 浏览器**（必须）

应用使用系统的 Google Chrome 浏览器来进行网页抓取。

**安装方法：**

```bash
# 方法一：从官网下载
访问 https://www.google.com/chrome/ 下载并安装

# 方法二：使用 Homebrew
brew install --cask google-chrome
```

**验证安装：**

```bash
# 检查 Chrome 是否已安装
ls -la "/Applications/Google Chrome.app"

# 查看版本
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --version
```

#### 2. **macOS 版本**

- **最低版本**: macOS 10.15 (Catalina) 或更高
- **推荐版本**: macOS 12 (Monterey) 或更高
- **支持架构**: Apple Silicon (M1/M2/M3) 或 Intel

---

## 📦 应用依赖说明

### 已内置的依赖

应用已经包含了以下所有必要的库和框架：

✅ **Electron 运行时** (41.0.1)
✅ **React 框架** (18.3.1)
✅ **Tailwind CSS** (UI 样式)
✅ **Puppeteer-Core** (浏览器自动化)
✅ **XLSX** (Excel 文件解析)
✅ **Node.js 运行环境**

### 系统级依赖

只需要安装 Google Chrome 浏览器即可：

- ✅ Google Chrome (推荐)
- 或 Chromium (替代方案)

---

## 🚀 安装步骤

### 第一步：安装 Google Chrome

#### 方法一：使用 Homebrew（推荐）

```bash
# 如果还没有安装 Homebrew，先安装 Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 安装 Google Chrome
brew install --cask google-chrome
```

#### 方法二：从官网下载

1. 访问 https://www.google.com/chrome/
2. 点击"下载 Chrome"按钮
3. 下载完成后双击安装包
4. 将 Chrome 拖拽到 Applications 文件夹

### 第二步：安装 Zara Scraper Pro

1. 双击 `Zara Scraper Pro-1.0.0-arm64.dmg`
2. 将应用拖拽到 Applications 文件夹
3. 在 Launchpad 或 Applications 中找到应用
4. 启动应用

### 第三步：验证安装

1. 启动 Zara Scraper Pro
2. 界面应该正常显示
3. 尝试输入一个款号进行测试
4. 点击"开始抓取"按钮

---

## 🔍 故障排除

### 问题 1：应用启动后无法抓取

**症状：**
- 界面正常显示，但点击"开始抓取"后报错
- 错误信息包含"找不到 Chrome"或"executable not found"

**解决方案：**

1. **检查 Chrome 是否已安装**

```bash
ls -la "/Applications/Google Chrome.app"
```

如果输出显示文件存在，说明已安装。

2. **验证 Chrome 可执行文件**

```bash
test -f "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" && echo "✅ Chrome 已安装" || echo "❌ Chrome 未安装"
```

3. **重新安装 Chrome**

如果未安装，请按上面的安装步骤安装 Chrome。

### 问题 2：Chrome 已安装但仍报错

**可能原因：**
- Chrome 安装路径不是默认路径
- Chrome 文件损坏
- 权限问题

**解决方案：**

1. **检查安装路径**

应用会检查以下路径：
- `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`
- `/Applications/Chromium.app/Contents/MacOS/Chromium`
- `/usr/bin/google-chrome`
- `/usr/bin/chromium-browser`

如果你的 Chrome 安装在其他位置，需要手动指定路径。

2. **卸载并重新安装 Chrome**

```bash
# 卸载 Chrome
sudo rm -rf "/Applications/Google Chrome.app"

# 重新安装（使用 Homebrew）
brew reinstall --cask google-chrome
```

3. **修复权限**

```bash
sudo chmod +x "/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
```

### 问题 3：应用无法启动

**症状：**
- 双击应用没有任何反应
- 或出现"已损坏"的警告

**解决方案：**

1. **允许未知开发者应用**

右键点击应用 → 选择"打开" → 在弹出的对话框中点击"打开"

2. **重置安全设置**

系统偏好设置 → 安全性与隐私 → 通用 → 允许从以下位置下载的应用 → "App Store 和被认可的开发者"

3. **重新下载和安装**

删除旧版本，重新安装。

### 问题 4：网络连接问题

**症状：**
- 抓取时出现网络错误
- 无法访问 Zara 网站

**解决方案：**

1. **检查网络连接**

确保你的 Mac 已连接到互联网。

2. **使用 VPN 或代理**

由于 Zara 美国官网可能有地区限制，建议使用美国 VPN 或代理。

3. **测试网络连接**

```bash
# 测试能否访问 Zara
curl -I https://www.zara.com
```

---

## 💡 优化建议

### 性能优化

1. **关闭不必要的应用**
   - 爬虫运行时关闭其他占用内存的应用

2. **调整并发数**
   - 低配电脑：标签页并发 2-3，下载并发 5-10
   - 高配电脑：标签页并发 5-10，下载并发 15-30

3. **使用有线网络**
   - 有线网络比无线网络更稳定

### 稳定性优化

1. **定期清理浏览器缓存**
   - 虽然应用使用独立的浏览器实例，但定期清理有助于性能

2. **分批抓取**
   - 单次建议不超过 50 个款号
   - 大量数据分批处理

3. **保持 Chrome 更新**
   - 确保使用最新版本的 Chrome

---

## 📋 依赖清单总结

### 用户需要安装的：

| 组件 | 版本要求 | 来源 | 必需 |
|------|---------|------|------|
| Google Chrome | 最新稳定版 | https://www.google.com/chrome/ | ✅ 是 |
| macOS | 10.15+ | Apple 系统 | ✅ 是 |

### 应用已包含的：

| 组件 | 版本 | 用途 |
|------|------|------|
| Electron | 41.0.1 | 应用框架 |
| React | 18.3.1 | UI 框架 |
| Tailwind CSS | 4.2.1 | UI 样式 |
| Puppeteer-Core | 24.39.0 | 浏览器自动化 |
| XLSX | 0.18.5 | Excel 解析 |

---

## 🎯 快速开始检查清单

- [ ] 已安装 Google Chrome 浏览器
- [ ] 已安装 Zara Scraper Pro 应用
- [ ] 已连接到互联网
- [ ] （可选）已配置 VPN 或代理
- [ ] 已准备好要抓取的款号或 Excel 文件
- [ ] 已选择输出目录（或使用默认的桌面文件夹）

全部完成后，就可以开始使用了！🚀
