# 🔄 更新说明 v1.0.1

## ✅ 已修复的问题

### 问题描述
之前版本的 Excel 文件选择功能只能选择文件夹，导致桌面上的 .xlsx 文件显示为灰色无法选中。

### 解决方案
- ✅ 添加了独立的文件选择功能
- ✅ Excel 文件选择器现在可以直接选择 .xlsx 和 .xls 文件
- ✅ 更新了 UI 标签，使按钮功能更清晰
- ✅ 修复了文件路径处理逻辑

---

## 🆕 新增功能

### 1. 文件选择器
- **Excel 文件选择**: 现在可以直接选择 Excel 文件
- **支持的格式**: .xlsx, .xls
- **过滤功能**: 文件对话框会自动过滤，只显示 Excel 文件

### 2. 改进的 UI 标签
- Excel 文件选择按钮：**"选择文件"**
- 输出目录选择按钮：**"选择文件夹"**
- 更清晰的功能说明

---

## 📝 使用方法更新

### Excel 文件导入（新方法）

#### 步骤 1：准备 Excel 文件
确保 Excel 文件格式正确：
- 款号在**第二列**（B 列）
- 第一行可以是标题

示例：

| A列 | B列（款号） | C列 |
|-----|-----------|-----|
| 品牌 | 款号 | 颜色 |
| ZARA | 0123456 | 黑色 |

#### 步骤 2：选择 Excel 文件

1. 点击 **"Excel 文件"** 部分的 **"选择文件"** 按钮
2. 在弹出的文件选择对话框中：
   - 找到你的 Excel 文件
   - 文件应该可以正常选中（不再显示为灰色）
   - 支持 `.xlsx` 和 `.xls` 格式
3. 点击"打开"
4. 文件路径会显示在输入框中

#### 步骤 3：开始抓取

点击 **"开始抓取"** 按钮，系统会自动：
- 解析 Excel 文件
- 提取第二列的所有款号
- 显示提取到的款号数量
- 开始抓取任务

### 输出目录选择（保持不变）

点击 **"输出目录"** 部分的 **"选择文件夹"** 按钮来选择图片保存位置。

---

## 🔍 技术细节

### 代码变更

#### 1. main.js
添加了新的 IPC 处理器：

```javascript
ipcMain.handle('select-file', async () => {
  const { canceled, filePaths } = await dialog.showOpenDialog({
    properties: ['openFile'],
    filters: [
      { name: 'Excel Files', extensions: ['xlsx', 'xls'] },
      { name: 'All Files', extensions: ['*'] }
    ]
  });
  return canceled ? null : filePaths[0];
});
```

#### 2. preload.js
暴露了新的 API：

```javascript
contextBridge.exposeInMainWorld('electronAPI', {
  // ... 其他 API
  selectFile: () => ipcRenderer.invoke('select-file'),
  // ...
});
```

#### 3. App.jsx
更新了文件选择处理函数：

```javascript
const handleSelectExcel = async () => {
  if (window.electronAPI) {
    const filePath = await window.electronAPI.selectFile();
    if (filePath) setExcelPath(filePath);
  }
};
```

---

## 📦 更新后的应用

### 版本信息
- **版本**: v1.0.1
- **文件**: `Zara Scraper Pro-1.0.0-arm64.dmg`
- **大小**: 约 119 MB
- **位置**: `/Users/jaylorandy/zara-app/release/`

### 安装方法

如果你已经安装了旧版本：

1. **卸载旧版本**
   ```bash
   # 删除旧的应用
   rm -rf /Applications/Zara\ Scraper\ Pro.app
   ```

2. **安装新版本**
   ```bash
   # 打开新的 DMG 文件
   open /Users/jaylorandy/zara-app/release/Zara\ Scraper\ Pro-1.0.0-arm64.dmg

   # 将应用拖拽到 Applications 文件夹
   ```

3. **启动应用**
   ```bash
   open -a "Zara Scraper Pro"
   ```

---

## ✨ 使用提示

### Excel 文件准备

✅ **正确的格式**:
- 款号在第二列（B 列）
- 第一行可以是标题
- 款号格式：7 位数字（如 0123456）

❌ **错误的格式**:
- 款号不在第二列
- 第一行不是标题但包含了款号
- 款号格式不正确（如带有字母、空格等）

### 文件选择对话框

✅ **现在的功能**:
- 可以直接选择 .xlsx 文件
- 可以直接选择 .xls 文件
- 自动过滤显示 Excel 文件
- 支持浏览文件夹结构

✅ **之前的限制**（已修复）:
- 只能选择文件夹
- Excel 文件显示为灰色
- 需要先选择文件夹再定位文件

---

## 🧪 测试建议

### 测试步骤

1. **准备测试文件**
   - 创建一个测试 Excel 文件
   - 在第二列输入 1-2 个款号
   - 保存为 .xlsx 格式

2. **测试文件选择**
   - 打开应用
   - 点击"Excel 文件"的"选择文件"按钮
   - 验证能否选中 Excel 文件
   - 确认文件路径正确显示

3. **测试抓取功能**
   - 点击"开始抓取"
   - 观察日志中的消息
   - 验证是否成功提取款号
   - 检查图片是否正常下载

4. **验证结果**
   - 检查输出目录
   - 确认图片按款式分类保存
   - 验证文件名格式正确

---

## 🐛 已知问题

无已知问题。

---

## 📞 反馈

如果在使用过程中遇到任何问题，请提供以下信息：

1. macOS 版本
2. Excel 文件格式
3. 错误日志内容
4. 操作步骤描述

---

## 🎉 总结

这次更新修复了 Excel 文件选择的用户体验问题，现在你可以：

✅ 直接选择 Excel 文件（无需先选择文件夹）
✅ 在文件对话框中看到清晰的文件过滤器
✅ 使用更直观的 UI 标签和按钮文字
✅ 享受更流畅的操作流程

感谢你的反馈，帮助我们改进产品！🚀
